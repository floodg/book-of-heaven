VITE_SUPABASE_URL=http://127.0.0.1:54331
VITE_SUPABASE_ANON_KEY=your-publishable-key-here

# Set to 'true' to switch from AnythingLLM (chat-proxy) to pgvector RAG (research-chat).
# Keep 'false' (or unset) until the corpus is ingested and research-chat is validated.
VITE_USE_PGVECTOR_RAG=false